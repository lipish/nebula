# Nebula centralized startup configuration
# Copy to deploy/nebula.env and edit values for your environment.

# Core endpoints
ETCD_ENDPOINT=http://127.0.0.1:2379
ROUTER_PORT=18081
GATEWAY_PORT=8081
BFF_PORT=18090
NODE_PORT=10824

# Node identity/model defaults
NODE_ID=node_gpu0
MODEL_UID=qwen2_5_0_5b
MODEL_NAME=Qwen/Qwen2.5-0.5B-Instruct

# BFF startup toggle in nebula-up.sh
START_BFF=1

# xtrace integration
XTRACE_URL=http://127.0.0.1:8742
# internal: no auth header first; service: require XTRACE_TOKEN
XTRACE_AUTH_MODE=service
XTRACE_TOKEN=replace-with-api-bearer-token
